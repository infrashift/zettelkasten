package index

import "testing"

func TestZettelValidation(t *testing.T) {
	// 1. Define a valid note map (matches CUE schema)
	// 2. Define an invalid note map (wrong project name)
	// 3. Assert that ValidateNote returns nil for (1) and an error for (2)
}
